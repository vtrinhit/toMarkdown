# FastAPI and server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.9.0
pydantic-settings>=2.5.0

# Async and concurrency
aiofiles>=24.1.0
httpx>=0.27.0

# Converters - Core (always available)
markitdown>=0.0.1a3
mammoth>=1.8.0
html2text>=2024.2.26
pypandoc>=1.14

# Converters - Optional (may require additional setup)
# Uncomment if needed:
# docling>=2.5.0          # IBM - requires additional deps
# marker-pdf>=1.0.0       # PDF OCR - requires torch
# unstructured>=0.15.0    # AI parsing - heavy deps

# PDF support
pdfplumber>=0.11.0

# OpenAI for AI-powered features
openai>=1.50.0

# Utils
python-magic>=0.4.27
# python-magic-bin>=0.4.14  # Uncomment for Windows (instead of python-magic)
pillow>=10.4.0

# OCR support (optional - requires system deps)
# pytesseract>=0.3.13
# pdf2image>=1.17.0
